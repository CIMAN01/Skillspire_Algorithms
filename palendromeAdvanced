/*
Improved version of palendrome using a faster algorithm.
*/

public class palendrome2 {

    public static boolean isPalendrome(String str) {
        // remove spaces from string and change to lower case
        str = str.replaceAll("\\s","").toLowerCase();
        // create starting and ending indexes
        int left = 0;
        int right = str.length()-1;
        // check the string for similar characters
        // (start at each end and meet in the middle while string has not been tested)
        while ((str.charAt(left) == str.charAt(right)) && (left < right)) {
            left++; // traverse right
            right--; // traverse left
        }
        // return false if string already tested
        if (left < right) {
            return false;
        }
        return true;
    }

    public static void main(String[] args) {
        // create a palendrome string
        String name = "Able was I ere I saw Elba";
        System.out.println();
        // invoke the method
        System.out.println(isPalendrome(name));
    }
}
